{
  "version": 2,
  "framework": "vite",
  "buildCommand": "pnpm run build",
  "installCommand": "pnpm i --frozen-lockfile",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}